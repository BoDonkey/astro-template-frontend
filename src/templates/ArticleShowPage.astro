---
import AposArea from '@apostrophecms/apostrophe-astro/components/AposArea.astro';
import dayjs from 'dayjs';

const { page, piece, user, query } = Astro.props.aposData;
---

<section class="main-content article-show-content">
  <h1 class="is-size-1">{ piece.title }</h1>
  <h4>
    Released On { dayjs(piece.publishedAt).format('MMMM D, YYYY') }
  </h4>
  <AposArea area={piece.mainContent} />
</section>
